# 理想规范态目录职责对照说明（Python 小白版）

> 写给小方：每个目录用"监考公司"打比方，对应你现在哪个文件

---

## 一句话理解整体结构

把你的项目想象成**一家监考公司**。公司有不同部门，各司其职：

```
一个 HTTP 请求进来 → 接待台(api)接单 → 业务部(services)编排
→ AI工程师团队(ml)跑模型 → 用表单(schemas)打包结果 → 返回给客户
```

**核心原则：每一层只做自己的事，不越界。**

---

## 逐个目录解释 + 文件对应

### 1. `app/main.py` —— 公司大门（启动入口）

**职责：** 创建 FastAPI 应用实例，把所有路由挂上去，启动服务。
**放什么：** 只有 `app = FastAPI(...)` 和路由注册，不写业务逻辑。

**对应你现在的文件：**
> `AiProctor0623/main.py`（56 行）

**变化：** 你现在的 main.py 里直接写了 3 个接口的逻辑。重构后，main.py 只负责"组装"，接口实现拆到 `api/` 目录。

```python
# 重构后的 main.py 长这样（只负责组装）
from fastapi import FastAPI
from app.api.v1 import proctor
from app.core.config import settings

app = FastAPI(title=settings.app_name)
app.include_router(proctor.router, prefix="/api/v1")
```

---

### 2. `app/api/v1/proctor.py` —— 接待台（接请求）

**职责：** 定义路由（`@router.get`、`@router.post`），接收请求，调用 services 层处理，返回结果。
**放什么：** 只有路由定义和参数校验，**不写业务逻辑**。

**对应你现在的文件：**
> `AiProctor0623/main.py` 里的 3 个接口函数

**变化：** 现在的接口里直接 `stProctor = ImageProctor()` 调模型。重构后，api 层只管"收请求 → 交给 service → 返结果"：

```python
# api/v1/proctor.py
@router.post("/upload_face")
async def upload_face(file: UploadFile):
    result = await proctor_service.analyze_face(file)  # 交给业务部
    return ApiResponse(data=result)                     # 用表单打包返回
```

**为什么叫 v1？** 接口版本号。以后升级 v2 时，v1 和 v2 能共存，不互相冲突。

---

### 3. `app/schemas/proctor.py` —— 表单模板（定格式）

**职责：** 用 Pydantic 定义"请求体"和"响应体"的结构。规定字段名、类型、是否必填。
**放什么：** 数据模型类，不写逻辑。

**对应你现在的文件：**
> **没有**。你现在直接用 `dict` 返回 `{"code": 0, "msg": "success", "data": ...}`，没有统一定义。

**为什么需要：** 现在 `/test` 返回 `{"code", "msg", "data"}`，`/ping` 返回 `{"pong", "msg"}`，格式不统一。定义一个 `ApiResponse` 模型，所有接口返回格式就一致了，前端不用猜。

```python
# schemas/proctor.py
from pydantic import BaseModel
from typing import Optional, Any

class ApiResponse(BaseModel):
    code: int = 0
    msg: str = "success"
    data: Optional[Any] = None

class FaceResult(BaseModel):
    """单次监考结果"""
    warnings: list[str] = []
    warning_count: int = 0
```

---

### 4. `app/services/proctor_service.py` —— 业务部（编排流程）

**职责：** 编排业务流程，协调 api 和 ml 之间的数据流转，处理异常。
**放什么：** 业务逻辑函数，比如"读取图片 → 调 ImageProctor → 整理结果"。

**对应你现在的文件：**
> **没有**。你现在 api 层直接调 `ImageProctor`，中间没有 service 层。

**为什么需要：** 现在接口里写了 `file_bytes = await file.read()` + `Image.open()` + `GetImageFaceAngleByImg()`。如果以后要加"图片大小限制""格式校验""日志记录"，这些逻辑堆在 api 里会很乱。放到 service 层，api 就干净了。

```python
# services/proctor_service.py
async def analyze_face(file: UploadFile) -> dict:
    # 1. 读取并校验
    file_bytes = await file.read()
    pil_img = Image.open(io.BytesIO(file_bytes))
    # 2. 调 AI 模型
    proctor = ImageProctor()
    result = proctor.get_image_face_angle_by_img(pil_img)
    # 3. 整理返回
    return {"warnings": result, "warning_count": len(result)}
```

---

### 5. `app/ml/` —— AI 工程师团队（跑模型）

**职责：** 所有机器学习相关代码。FaceMesh 人脸检测、Pose 动作识别、YOLO 物体检测。
**放什么：** 检测器类、模型加载、推理逻辑。

**对应你现在的文件：**

| 新文件 | 对应现有文件 | 说明 |
|--------|-------------|------|
| `app/ml/image_proctor.py` | `AiProctor/Logic/ImageProctor.py` | 图片监考（核心，432行） |
| `app/ml/front_camera.py` | `AiProctor/Logic/FrontCamera.py` | 前置摄像头实时监考 |
| `app/ml/back_camera.py` | `AiProctor/Logic/BackCamera.py` | 后置摄像头（YOLO） |
| `app/ml/toolkit.py` | `AiProctor/Logic/Toolkit.py` | 工具函数（写中文文字） |
| `app/ml/base_proctor.py` | **无（新建）** | 公共基类，消除重复代码 |

**base_proctor.py 是什么？** 你的 `ImageProctor` 和 `FrontCamera` 里有大量重复代码（阈值定义、`__GetFaceAngle`、`__CheckDirection`、`__Checkindependence` 几乎一模一样）。基类把这些公共逻辑抽出来，两个子类继承它，只写差异部分。改一处 = 两处都生效。

---

### 6. `app/core/config.py` —— 行政部（配置管理）

**职责：** 集中管理所有配置项——端口号、模型路径、检测阈值等。
**放什么：** 用 `pydantic-settings` 读取 `.env` 文件的配置类。

**对应你现在的文件：**
> **没有**。你现在所有阈值硬编码在 `ImageProctor.__init__()` 里（`m_iMaxLeftAngle = 6` 这种）。

**为什么需要：** 调阈值要改代码、重新部署。放到 `.env` 后，改配置不用动代码，重启即可。不同考试场景可以用不同配置文件。

```python
# core/config.py
from pydantic_settings import BaseSettings

class Settings(BaseSettings):
    app_name: str = "AiProctor 智能监考"
    host: str = "0.0.0.0"
    port: int = 8000
    # 人脸角度阈值
    max_left_angle: int = 6
    max_right_angle: int = -6
    # Pose 阈值
    phone_wrist_ear_dist: float = 0.35
    # 模型路径
    model_path: str = "models/face_landmarker.task"

    class Config:
        env_file = ".env"

settings = Settings()
```

```bash
# .env.example（配置模板）
APP_NAME=AiProctor 智能监考
MAX_LEFT_ANGLE=6
PHONE_WRIST_EAR_DIST=0.35
MODEL_PATH=models/face_landmarker.task
```

---

### 7. `scripts/` —— 工具间（不常用脚本）

**职责：** 放调试、标定、采集用的脚本。这些脚本不参与正式服务运行。
**放什么：** 一次性工具脚本。

**对应你现在的文件：**

| 新位置 | 现有文件 | 用途 |
|--------|---------|------|
| `scripts/capture_samples.py` | 根目录 `capture_samples.py` | 拍照采集样本 |
| `scripts/probe_keypoints.py` | 根目录 `probe_keypoints.py` | 关键点探测 |
| `scripts/verify_actions.py` | 根目录 `verify_actions.py` | 动作验证 |
| `scripts/image_proctor_annotated.py` | 根目录 `ImageProctor_annotated.py` | 带注释版 |
| `scripts/aiproctor_demo.py` | `AiProctor/AiProctor.py` | 摄像头演示 |
| `scripts/aiproctor2_demo.py` | `AiProctor/AIProctor2.py` | 图片演示 |

---

### 8. `tests/` —— 质检部（测试）

**职责：** 用 pytest 写自动化测试，保证改代码不改坏。
**放什么：** `test_*.py` 测试文件。

**对应你现在的文件：**
> 只有 `AiProctor0623/test_accuracy.py`（非标准 pytest 格式），需要规范化。

```python
# tests/test_proctor.py 示例
def test_normal_face():
    """测试正常考试姿态"""
    proctor = ImageProctor()
    result = proctor.get_image_face_angle("assets/test_images/normal.jpg")
    assert "正常" in result[0][0]

def test_phone_action():
    """测试打电话检测"""
    result = proctor.get_image_face_angle("assets/test_images/phone.jpg")
    assert "打电话" in result[0][0]
```

---

### 9. `models/` —— 模型仓库

**职责：** 统一存放模型文件（.task、.pt）。
**对应现有：** `face_landmarker.task`（3.7MB）+ `AiProctor/Weights/yolo11n.pt`

---

### 10. `assets/` —— 素材库

**职责：** 存放静态资源——字体、测试图片。
**对应现有：** `AiProctor/Fonts/` + `AiProctor/test_images/`

---

### 11. 根目录配置文件

| 文件 | 作用 | 现状 |
|------|------|------|
| `.env.example` | 环境变量模板（提交到 git） | 需新建 |
| `.env` | 实际环境变量（不提交） | 需新建 |
| `pyproject.toml` | 依赖声明（唯一来源） | 有，需更新 |
| `uv.lock` | 依赖版本锁定（**必须提交**） | 有，但被 .gitignore 忽略！ |
| `Dockerfile` | 容器构建 | 有，需更新路径 |
| `README.md` | 项目文档 | 需新建 |

---

## 文件映射总表（一张表看全）

| 现有文件 | → 新位置 | 备注 |
|---------|---------|------|
| `AiProctor0623/main.py` | `app/main.py` | 重写为分层入口 |
| `AiProctor0623/AiProctor/Logic/ImageProctor.py` | `app/ml/image_proctor.py` | 核心逻辑，改命名+import |
| `AiProctor0623/AiProctor/Logic/FrontCamera.py` | `app/ml/front_camera.py` | 改命名+import |
| `AiProctor0623/AiProctor/Logic/BackCamera.py` | `app/ml/back_camera.py` | 改命名+import |
| `AiProctor0623/AiProctor/Logic/Toolkit.py` | `app/ml/toolkit.py` | 改命名+import |
| `AiProctor0623/AiProctor/Logic/ImageProctor.py.bak` | **删除** | 有 git 不需要 .bak |
| `AiProctor0623/AiProctor/AiProctor.py` | `scripts/aiproctor_demo.py` | 移到脚本目录 |
| `AiProctor0623/AiProctor/AIProctor2.py` | `scripts/aiproctor2_demo.py` | 移到脚本目录，修 str bug |
| `capture_samples.py` | `scripts/capture_samples.py` | 移到脚本目录 |
| `probe_keypoints.py` | `scripts/probe_keypoints.py` | 移到脚本目录 |
| `verify_actions.py` | `scripts/verify_actions.py` | 移到脚本目录 |
| `ImageProctor_annotated.py` | `scripts/image_proctor_annotated.py` | 移到脚本目录 |
| `AiProctor0623/face_landmarker.task` | `models/face_landmarker.task` | 移到模型目录 |
| `AiProctor0623/AiProctor/Weights/` | `models/weights/` | 移到模型目录 |
| `AiProctor0623/AiProctor/Fonts/` | `assets/fonts/` | 移到素材目录 |
| `AiProctor0623/AiProctor/test_images/` | `assets/test_images/` | 移到素材目录 |
| —（无） | `app/api/v1/proctor.py` | **新建**：路由层 |
| —（无） | `app/schemas/proctor.py` | **新建**：数据模型 |
| —（无） | `app/services/proctor_service.py` | **新建**：业务编排 |
| —（无） | `app/core/config.py` | **新建**：配置管理 |
| —（无） | `app/ml/base_proctor.py` | **新建**：公共基类 |
| —（无） | `tests/test_proctor.py` | **新建**：测试 |
| —（无） | `.env.example` | **新建**：配置模板 |
| —（无） | `README.md` | **新建**：项目文档 |
| `AiProctor0623/requirements.txt` | **删除** | pyproject.toml 替代 |
| `AiProctor0623/.venv/` | **删除** | 根目录重建 .venv |

---

## 改动量评估

| 类别 | 数量 | 说明 |
|------|------|------|
| 移动文件 | 12 个 | 物理搬运，不改内容 |
| 新建文件 | 8 个 | 分层骨架 + 配置 + 测试 + 文档 |
| 删除文件 | 3 个 | requirements.txt、.bak、重复 .venv |
| 改 import | 5 个文件 | 路径变了，import 要跟着改 |
| 改命名 | 全局 | 匈牙利命名 → PEP 8（可选，不影响运行） |

**总工作量：约 3-4 小时**（含调试 import 错误）
