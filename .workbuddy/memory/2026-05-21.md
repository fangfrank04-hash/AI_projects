# 2026-05-21 工作记录

## 评测集需求分析

**任务**：为内网 ragflow 改写项目添加评测集 Python 接口

**参考文章**：https://cloud.tencent.com/developer/article/2651859（RAGAS + LangFuse 评测实战）

**现状盘点**：
- `api/db/db_models.py` 已有四张评测表：`EvaluationDataset`、`EvaluationCase`、`EvaluationRun`、`EvaluationResult`
- `api/db/services/evaluation_service.py` 已有骨架代码，但 LLM-as-judge 指标（Faithfulness、Answer Relevancy、Context Precision/Recall）均标注 TODO，未实现
- `api/apps/restful_apis/` 中**没有** `evaluation_api.py`，即评测的 HTTP 接口层完全缺失
- ragflow 已集成 LangFuse（`langfuse_api.py` + `langfuse_service.py`），可复用

**需要新增的工作**：
1. 实现 `evaluation_service.py` 中的 LLM-as-judge 指标（Faithfulness、Context Precision、Context Recall、Answer Relevancy）
2. 新增 `api/apps/restful_apis/evaluation_api.py`，暴露完整 RESTful 接口
3. 注册路由到 `ragflow_server.py`
4. 补充数据库迁移 SQL（如需）

**技术方向**：不引入 RAGAS 库（内网可能无法安装），改用 LLM-as-judge 方式用 ragflow 自身的 LLM 接口实现四大指标
