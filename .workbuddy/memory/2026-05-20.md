# 2026-05-20

## 项目依赖分析 & 内网上传打包方案
- 全面分析了三个模块的完整依赖树：
  - **Python (proposal-python-v2)**：7 直接依赖 → 86 传递依赖
  - **React 前端 (react-frontend)**：10 直接依赖 → 181 node_modules 包（85MB）
  - **Java (proposal-java)**：15 直接依赖 → ~90-110 传递 jars
- Java Maven 无法运行（系统 JDK 1.6 太旧），需要 JDK 17 环境来构建 Fat JAR

## 实际下载依赖到项目目录
- 创建了 `项目依赖/` 目录，内含三个子文件夹：Python依赖、前端依赖、Java依赖
- Python依赖：重新下载并解压为 Python 3.12 版本（cp312-win_amd64），直接是 site-packages 格式，236项，~200MB
- 前端依赖：已复制 node_modules（85MB）到 `项目依赖/前端依赖/`
- Java依赖：写了 README.txt 说明需要 JDK 17 + mvn package
- 重写了《内网上传依赖打包清单.md》为 PyCharm 小白版：
  - Python: 解释清楚了解释器/虚拟环境/site-packages 概念；操作 = Ctrl+A 全选 Python依赖 → 粘贴到内网 Lib\site-packages\
  - 前端: 复制 node_modules + npm run dev 启动
  - Java: java -jar 直接运行（需要 JDK 17 jar）

## 端到端运行验证 ✅
- 启动三服务全链路联调成功：
  - MCP 数据服务 (8001)：FastMCP Streamable HTTP，正常响应
  - FastAPI 主服务 (8000)：health check OK，SSE 流式端点正常
  - React 前端 (5173)：Vite 开发服务器正常
- SSE 事件流完整验证：
  - connected → preview(项目数据) → text(AI 欢迎语)
  - 发送消息 "查询项目基本信息" → AI 成功返回格式化项目信息
  - 发送消息 "把产品名称改为..." → AI 成功处理修改请求
- Java 服务仍无法启动（JDK 1.6），需要 JDK 17

## 2026-05-21：重新启动服务 + 排查 SSE 连接错误
- 上次验证后 taskkill 清理了所有进程 → 用户打开页面时报 "SSE连接错误"
- 按顺序重启三服务：MCP(8001) → FastAPI(8000) → Vite(5173)
- 问题根因：后台任务默认 2 分钟超时自动结束，需用 timeout=600000
- 重启后 SSE 全链路验证通过：connected → preview → text(AI 欢迎语) → text(AI 查询结果)
- FastAPI 日志显示前端页面 "陈杰" 已自动建立 SSE 连接（2 个活跃会话）
- 服务列表：Aqr9bA(MCP), nwnbsv(FastAPI), NyaIDp(Vite)

## 一键启动脚本 & 服务保活方案
- 创建了 `启动全部服务.bat`，用户双击即可启动全部 3 个服务
- 核心方案：用 `start "标题" cmd /c ...` 为每个服务打开独立 CMD 窗口
  - MCP(8001) → FastAPI(8000) → React(5173)，按依赖顺序，中间各等 5-8 秒
  - 独立窗口 = 进程不会被父进程杀死，一直保持运行
  - 不用时手动关掉 CMD 窗口即可
- 项目根目录路径：D:\AI_projects\zhongzhai_pro\project-proposal-ai
