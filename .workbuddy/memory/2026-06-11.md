# 2026-06-11 工作日志

## 框架分析：FastAPI + AgentScope + FastMCP 架构评估

### 完成内容
- 全面分析了 proposal-python-v2 项目的框架、Agent 定义、通信链路和设计模式
- 阅读了 agent_setup.py (632行)、main.py (349行)、config.py、http_server.py (587行) 及两个 SKILL.md
- 确认项目使用自定义编排模式（非 AgentScope 原生 pipeline）

### 关键发现
- 3个Agent：IntentRouter (qwen-plus/1轮) → ProjectAgent (qwen-max/10轮/toolkit) → TeamAgent (qwen-max/10轮/toolkit)
- 信息传递：Router 分类 int → 分发到 Worker Agent → Agent 调 MCP 工具 → Queue → SSE → 前端
- 不使用 SequentialPipeline/MsgHub，完全由 ProjectAssistantAgent 类手工编排
- DEV_MODE 双模式（mock/prod）切换良好
- "预览-回填"模式保证写操作安全

### 待改进
- agent_setup.py 过于庞大（632行），建议拆分为 agents/router.py、agents/project_agent.py、agents/team_agent.py 等
- 双层意图判断（Router + Worker 各自判断）存在冗余
