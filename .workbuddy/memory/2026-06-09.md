# 2026-06-09 工作日志

## 团队职责修改预览 — 修复 LLM 不调包装器的问题

### 问题
`_tool_update_duty` 包装器（已实现预览+推送 teamPreview 逻辑）不被 LLM 调用，
因为 MCP 的 `update_duty` 工具名更清晰，LLM 优先选它。

### 修复
在 TeamAgent 的 `ttk.register_mcp_client` 中，给 `disable_funcs` 加上 `"update_duty"` 和 `"add_team_member"`，
隐藏同名 MCP 工具，强制 LLM 调用本地包装器。

### 改动文件
- `agent_setup.py:187`: `disable_funcs` 增加 `update_duty`, `add_team_member`
