# 2026-06-04

## 新增：模式3 fetch + ReadableStream（方案B — 保留旧代码）
- **main.py**: 新增 `POST /api/chat` 流式聊天路由（~50行），新增 `POST /api/chat/fillback-v3` 无session回填路由。旧的路由（GET /api/chat/stream, POST /api/chat/message, POST /api/chat/fillback）全部保留。
- **agent_setup.py**: 完全不动
- **streamClient.js**: 新建前端客户端，用 fetch + ReadableStream 替代 EventSource
- **App.jsx**: 切换调用方式（sseClient → streamClient），handleSend/回填按钮均改用新客户端
- `__INIT__` 特殊消息：仅触发 Agent 初始化加载预览数据，不触发消息处理避免重复问候

## 15:19-15:54 项目全面架构分析
完成了对项目的框架、Skills 设计、Agent 架构的全面分析（含联网搜索对比主流实践）：
- **框架**：React 18 + FastAPI + AgentScope + MCP SDK + Spring Boot + 通义千问(DashScope)
- **Skills**：6 个，按 PRD 5 步业务流程划分（V1 只接入前 2 个）
- **Agent**：3 个（IntentRouter + ProjectAgent + TeamAgent），Router→Worker 分发架构
- **对比结论**：按业务步骤划分 Skills、2-5 Agent 规模均属主流合理设计，符合 Anthropic Orchestrator-Subagent 模式
