# 2026-06-08

## BugFix: 修复"查看项目信息"不返回 JSON 格式 data 事件

- **根因**: `_auto_push_tool_data` 依赖 AgentScope ReActAgent 的 response.content 中包含 `tool_result` block，但 ReActAgent 内部循环消费掉中间结果，最终响应不含这类 block
- **修复**: 新增 `_push_structured_data_by_intent()` 方法，直接通过 MCP 客户端调 `get_project_info`/`get_team_members_list` 获取数据并推送 JSON
- **文件**: `proposal-python-v2/agent_setup.py` (第338行替换调用，第443-464行新增方法)

## 功能增强: 团队职责修改改为先推预览再暂存参数

- 查询团队职责改用 `get_user_info` 接口（返回含 endList/list 的原始数据）
- `_tool_update_duty` 重写：查当前 → _compute_duty_diff 内存模拟 → 推 teamPreview → 存 _pending_duty_params → 注释掉 Java 调用
- 新增 `_compute_duty_diff` 方法：在内存中修改 endList 并生成 roleList
- `_do_fillback` 中推送 dutyParams（roleList + pid），前端拿到后调 Java
- 全部改动在 `agent_setup.py`（6处），不动 `http_server.py`
