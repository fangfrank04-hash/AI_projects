# 2026-06-10 工作日志

## 团队职责修改预览功能完成

最终方案总结：
1. **`_tool_update_duty`**（包装器）：LLM 调它生成预览，推 `teamData`（修改后预览数据）
2. **`_push_structured_data_by_intent`**：查询时推 `teamData`（数据库数据），`_pending_duty_params` 非空时跳过
3. **`handle_message`**：`team_management` 的 `_push_structured_data_by_intent` 从第 2 步移到 Agent 运行后，根据 `_pending_duty_params` 状态决定是否推

最终数据流：
- 查询 → 推 `teamData` ✅
- 修改 → `_tool_update_duty` 推 `teamData`（预览数据）✅ + `_pending_duty_params` 跳过后续推送

### 改动文件
- `agent_setup.py`：
  - `_init_team_agent`：`_compute_duty_diff` 内联到 `_tool_update_duty`
  - `disable_funcs`：两个 Agent 的 MCP 注册加上 `update_duty` 和 `add_team_member`
  - `handle_message`：`team_management` 的提前推送移到 Agent 运行后
  - `_tool_update_duty`：推 `teamData` 而不是 `teamPreview`
