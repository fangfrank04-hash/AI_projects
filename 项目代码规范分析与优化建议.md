# AiProctor0623 项目代码规范分析与优化建议

> 分析时间：2026-07-02  
> 分析对象：AI 智能监考服务（FastAPI + YOLO11 + MediaPipe）  
> 面向读者：Python 开发初学者

---

## 一、总体评价

先说结论：**作为第一个 Python 项目，能跑起来、能交付，已经很不错了。** 功能逻辑（人脸角度检测、Pose 动作识别）写得是有条理的，阈值标定也做得很扎实。

但从「工程规范」角度看，目前更像是「能跑的脚本」而不是「可维护的项目」。主要问题集中在 5 个方面：

| 维度 | 现状评分 | 说明 |
|------|---------|------|
| 目录结构 | ⭐⭐ | 嵌套冗余、脚本散落、大文件混入代码 |
| 依赖管理 | ⭐⭐ | 两套依赖文件不同步、lock 文件被忽略 |
| 代码规范 | ⭐⭐⭐ | 命名风格、重复代码、硬编码 |
| 接口结构 | ⭐⭐ | 无分层、无配置、无异常处理 |
| 工程化 | ⭐ | 无测试、无 CI、无文档 |

下面逐项展开，每个问题都附上「为什么是问题」和「怎么改」。

---

## 二、目录结构问题

### 问题 1：嵌套同名目录（严重）

**现状：**
```
AiProctor0623/              ← 仓库根目录
├── AiProctor0623/          ← 实际代码在这（又一层同名目录！）
│   ├── main.py
│   ├── AiProctor/
│   └── ...
├── capture_samples.py      ← 调试脚本散落在根目录
├── probe_keypoints.py
├── verify_actions.py
└── ImageProctor_annotated.py
```

**为什么是问题：** 路径多套一层，import、Docker COPY、运行命令都得带着这层冗余目录。`main.py` 不在根目录，每次运行得先 `cd` 进去。

**怎么改：** 把 `AiProctor0623/AiProctor0623/` 里的内容提到仓库根目录，去掉中间这层。

### 问题 2：调试脚本散落在根目录（中等）

**现状：** 根目录散落着 `capture_samples.py`、`probe_keypoints.py`、`verify_actions.py`、`ImageProctor_annotated.py` 等脚本。

**为什么是问题：** 这些是调试/标定用的临时脚本，和正式服务代码混在一起，分不清哪些是「产品代码」哪些是「工具脚本」。

**怎么改：** 统一放到 `scripts/` 目录，并在文件头注释说明用途。

### 问题 3：.venv 虚拟环境重复存在（严重）

**现状：** 根目录有一个 `.venv/`，`AiProctor0623/AiProctor0623/` 下又有一个 `.venv/`。两个虚拟环境，占双倍磁盘空间，还容易搞混「我到底在用哪个」。

**为什么是问题：** 虚拟环境应该整个项目只有一个，放在根目录。而且 `.venv/` 不应该提交到 Git（虽然 `.gitignore` 里有写，但子目录的可能已被追踪）。

**怎么改：** 删掉其中一个，只保留根目录的 `.venv/`。

### 问题 4：大模型文件直接放代码目录（中等）

**现状：** `face_landmarker.task`（3.7MB）直接放在代码目录里，会跟着 Git 提交。

**为什么是问题：** Git 不适合管理大文件，仓库会越来越臃肿，clone 变慢。

**怎么改：** 用 Git LFS 管理，或者放到 `assets/` / `models/` 目录并在 README 说明从哪下载。

### 问题 5：备份文件 .bak 留在代码里（轻微）

**现状：** `ImageProctor.py.bak` 备份文件留在 `Logic/` 目录。

**为什么是问题：** 有 Git 版本控制就不需要手动 .bak 文件。留着会造成混乱（到底改哪个？）。

**怎么改：** 删掉 `.bak` 文件，历史版本用 Git 恢复。

### 问题 6：.idea / __pycache__ 可能已被追踪（轻微）

**现状：** `.gitignore` 里写了忽略 `.idea/` 和 `__pycache__/`，但 `AiProctor/.idea/` 目录存在，说明可能在这些规则生效前就已被提交。

**怎么改：** 用 `git rm -r --cached` 清理已追踪的这些目录。

---

## 三、依赖管理问题

### 问题 7：requirements.txt 和 pyproject.toml 不同步（严重）

**现状：**
- `requirements.txt`（5 个包）：缺 `python-multipart`
- `pyproject.toml`（6 个包）：有 `python-multipart`
- Dockerfile 里又单独 `pip install python-multipart` 补漏

**为什么是问题：** 两套依赖文件，改了一个忘改另一个，就会出 bug。Dockerfile 里打补丁说明已经踩过这个坑了。

**怎么改：** 统一用 `pyproject.toml` 作为唯一依赖来源，删掉 `requirements.txt`（或用 `uv export` 自动生成）。既然已经在用 `uv`，就彻底拥抱它。

### 问题 8：uv.lock 被 .gitignore 忽略（严重）

**现状：** `.gitignore` 第 42 行写了 `uv.lock`，把锁文件忽略了。

**为什么是问题：** **这是大问题。** lock 文件的作用是锁定所有依赖的精确版本，保证「我这里能跑，你那里也能跑」。忽略它 = 放弃可复现构建。今天装是 mediapipe 0.10.20，明天别人装可能变成别的版本，行为就不一样了。

**怎么改：** 从 `.gitignore` 删掉 `uv.lock`，把它提交进 Git。`requirements.txt` 可以忽略，但 lock 文件必须提交。

### 问题 9：直接依赖没写全（中等）

**现状：** 代码里直接用了 `cv2`、`numpy`、`PIL`（Pillow），但 `requirements.txt` / `pyproject.toml` 里没显式声明它们，只是靠 `mediapipe` / `opencv-python` 顺带装上的。

**为什么是问题：** 「间接依赖」是别人顺带给你的，哪天 mediapipe 升级不再依赖某个版本，你的代码就崩了。用到什么包就应该显式声明什么包。

**怎么改：** 把 `numpy`、`pillow` 显式加到依赖里。

### 问题 10：Dockerfile 没用 uv（中等）

**现状：** Dockerfile 用 `pip install` 装依赖，但你本地用的是 `uv`。

**为什么是问题：** uv 比 pip 快 10-100 倍，而且能利用 lock 文件保证版本一致。用 pip 装就丢掉了 lock 的好处。

**怎么改：** Dockerfile 改用 uv：
```dockerfile
FROM python:3.10.20-slim
COPY --from=ghcr.io/astral-sh/uv:latest /uv /usr/local/bin/uv
COPY pyproject.toml uv.lock ./
RUN uv sync --frozen --no-dev
```

### 问题 11：版本号风格不统一（轻微）

**现状：** 有的精确锁定（`mediapipe==0.10.20`），有的用范围（`fastapi>=0.138.0`）。

**怎么改：** 直接依赖用 `>=` 范围声明在 pyproject.toml，精确版本交给 lock 文件。不要手动写 `==`。

---

## 四、代码规范问题

### 问题 12：匈牙利命名法（严重）

**现状：** 变量名带类型前缀：
- `m_iMaxLeftAngle`（m=成员，i=int）
- `stProctor`（st=结构体？）
- `in_strImagePath`（in=输入参数，str=字符串）
- `listFace2d`、`bPoseActionDetected`

**为什么是问题：** 这是 C++/Delphi 的命名习惯，Python 完全不这么写。PEP 8（Python 代码规范）推荐用 `snake_case` 小写加下划线，不加类型前缀。Python 有类型注解，不需要在变量名里写类型。

**怎么改：**
```python
# 不要这样
m_iMaxLeftAngle = 6
in_strImagePath: str

# 要这样
max_left_angle = 6
image_path: str
```

### 问题 13：变量名覆盖内置函数（严重）

**现状：** `AIProctor2.py` 第 13 行：
```python
str = stProctor.GetImageFaceAngle('test_images/person2.jpg')
```

**为什么是问题：** `str` 是 Python 内置类型（字符串），被你当成变量名用后，这个作用域里 `str()` 函数就被覆盖了，后面再想用 `str(123)` 就会报错。

**怎么改：** 改成 `result` 或 `face_result`。

### 问题 14：拼写错误（轻微但应修）

**现状：**
- `stSlutionDraw` → 应为 `SolutionDraw`
- `stRrawStyle` → 应为 `RawStyle`

**怎么改：** 全局替换修正。

### 问题 15：ImageProctor 和 FrontCamera 大量重复代码（严重）

**现状：** `ImageProctor` 和 `FrontCamera` 里的 `__GetFaceAngle`、`__CheckDirection`、`__Checkindependence` 方法几乎一模一样。

**为什么是问题：** 重复代码 = 改一处忘改另一处 = bug。维护成本翻倍。

**怎么改：** 抽一个基类 `BaseProctor`，把公共逻辑放基类，子类只实现差异部分。

### 问题 16：魔法数字硬编码（中等）

**现状：** 关键点编号 `33, 263, 1, 61, 291, 199` 直接写在代码里，没有注释说明每个点代表什么。

**为什么是问题：** 过两个月你自己都不知道 33 是左眼还是右眼。

**怎么改：** 定义常量：
```python
# MediaPipe Face Mesh 关键点索引
LANDMARK_LEFT_EYE = 33
LANDMARK_RIGHT_EYE = 263
LANDMARK_NOSE = 1
# ...
```

### 问题 17：用 print 代替日志（中等）

**现状：** 全程用 `print()` 输出调试信息。

**为什么是问题：** print 无法控制级别（DEBUG/INFO/WARNING/ERROR），无法写到文件，生产环境没法关闭。

**怎么改：** 用 `logging` 模块：
```python
import logging
logger = logging.getLogger(__name__)
logger.info("识别成功")
logger.error("图片加载失败: %s", path)
```

### 问题 18：阈值硬编码在代码里（中等）

**现状：** 所有检测阈值（角度范围、距离阈值等）都写死在 `__init__` 里。

**为什么是问题：** 调阈值要改代码、重新部署。不同考试场景可能要不同阈值。

**怎么改：** 用配置文件（YAML/JSON）或 `.env` 环境变量管理，用 `pydantic-settings` 读取。

### 问题 19：GUI 逻辑混在服务代码里（中等）

**现状：** `ImageProctor.Start()` 方法调用 `cv2.imshow()` 弹窗显示图片。但这是 API 服务，服务器上没有显示器，调用 imshow 会直接报错。

**怎么改：** 把 GUI 展示逻辑拆到单独的脚本，服务代码只做检测不弹窗。

---

## 五、接口结构问题

### 问题 20：FastAPI 没有分层（严重）

**现状：** 所有路由直接写在 `main.py`，没有用 `APIRouter` 拆分，没有 schemas（Pydantic 模型），没有 services 层。业务逻辑和路由混在一起。

**为什么是问题：** 接口一多就全堆在一个文件里，没法维护。

**怎么改：** 标准分层结构：
```
app/
├── api/          # 路由层（接收请求）
│   └── v1/
│       └── proctor.py
├── schemas/      # 数据模型（请求/响应结构）
│   └── proctor.py
├── services/     # 业务逻辑层
│   └── proctor.py
└── core/         # 配置、异常处理
    ├── config.py
    └── exceptions.py
```

### 问题 21：没有统一响应格式（中等）

**现状：**
- `/test` 返回 `{"code": 0, "msg": "识别成功", "data": ...}`
- `/ping` 返回 `{"pong": True, "msg": "server is alive"}`

格式不统一。

**怎么改：** 定义统一响应模型：
```python
from pydantic import BaseModel
from typing import Optional, Any

class ApiResponse(BaseModel):
    code: int = 0
    msg: str = "success"
    data: Optional[Any] = None
```

### 问题 22：没有异常处理（严重）

**现状：** `/upload_face` 接口里，如果上传的不是图片、或图片损坏，`Image.open()` 会抛异常，直接返回 500 错误，前端拿到的是一坨报错信息。

**怎么改：** 加 try/except + 全局异常处理器：
```python
@app.exception_handler(Exception)
async def global_exception_handler(request, exc):
    return ApiResponse(code=-1, msg=str(exc), data=None)
```

### 问题 23：没有输入验证（中等）

**现状：** 上传文件没有限制大小、没有限制类型（传个 .exe 也能进来）。

**怎么改：** 用 Pydantic + FastAPI 的依赖注入做文件校验。

### 问题 24：API 没有版本前缀（轻微）

**现状：** 接口路径是 `/test`、`/upload_face`，没有 `/api/v1/` 前缀。

**为什么是问题：** 以后接口升级（v2）时没法共存。

**怎么改：** 路由统一加 `/api/v1` 前缀。

### 问题 25：app.title 还是占位符（轻微）

**现状：** `title="111"`，是随手填的占位符没改。

**怎么改：** 改成正式名称，因为这是 Swagger 文档页的标题。

---

## 六、工程化问题

### 问题 26：没有测试（严重）

**现状：** 没有标准的 `pytest` 测试，只有一些散落的验证脚本。

**怎么改：** 建 `tests/` 目录，用 pytest 写测试：
```
tests/
├── conftest.py
├── test_proctor.py
└── test_api.py
```

### 问题 27：没有 README（中等）

**现状：** 项目没有 README，新人接手不知道怎么跑。

**怎么改：** 写一个 README.md，包含：项目简介、环境要求、安装步骤、运行命令、接口文档链接。

### 问题 28：没有代码格式化/lint 工具（中等）

**现状：** 没有 `ruff`、`black` 等代码规范工具，代码风格靠手动维持。

**怎么改：** 加 `ruff`（集成了 lint + format）到 dev 依赖，配 pre-commit 钩子。

### 问题 29：没有 .env 配置管理（中等）

**现状：** 没有 `.env` 文件，配置散落在代码里。

**怎么改：** 用 `pydantic-settings` 管理配置：
```python
# .env
APP_NAME=AiProctor
MAX_LEFT_ANGLE=6
MODEL_PATH=models/face_landmarker.task
```

---

## 七、推荐的项目结构

```
AiProctor0623/                    # 仓库根目录
├── app/                          # 应用主包
│   ├── __init__.py
│   ├── main.py                   # FastAPI 入口
│   ├── api/                      # 路由层
│   │   ├── __init__.py
│   │   ├── deps.py               # 依赖注入
│   │   └── v1/
│   │       ├── __init__.py
│   │       └── proctor.py        # 监考相关接口
│   ├── core/                     # 核心配置
│   │   ├── config.py             # 配置管理（pydantic-settings）
│   │   └── exceptions.py         # 自定义异常
│   ├── schemas/                   # 数据模型
│   │   └── proctor.py             # 请求/响应模型
│   ├── services/                  # 业务逻辑层
│   │   └── proctor_service.py
│   └── ml/                        # 机器学习模块
│       ├── __init__.py
│       ├── base_proctor.py        # 监考基类（抽取公共逻辑）
│       ├── image_proctor.py       # 图片监考
│       └── pose_detector.py       # Pose 动作检测
├── assets/                       # 静态资源
│   ├── fonts/
│   └── test_images/
├── models/                       # 模型文件
│   └── face_landmarker.task       # （用 Git LFS 管理）
├── scripts/                      # 工具脚本
│   ├── capture_samples.py
│   ├── probe_keypoints.py
│   └── calibrate_threshold.py
├── tests/                        # 测试
│   ├── conftest.py
│   ├── test_api.py
│   └── test_proctor.py
├── .env.example                  # 环境变量模板
├── .gitignore
├── pyproject.toml                # 依赖管理（唯一来源）
├── uv.lock                       # 锁文件（必须提交）
├── Dockerfile
├── docker-compose.yml
└── README.md
```

---

## 八、优化行动清单（按优先级排序）

### P0 - 立即修（影响项目正确性）

| # | 任务 | 工作量 |
|---|------|--------|
| 1 | 把 `uv.lock` 从 `.gitignore` 移除并提交 | 5 分钟 |
| 2 | 统一依赖到 `pyproject.toml`，删掉 `requirements.txt` | 15 分钟 |
| 3 | 补全直接依赖（numpy、pillow 显式声明） | 5 分钟 |
| 4 | 修 `AIProctor2.py` 里 `str = ...` 覆盖内置函数 | 2 分钟 |
| 5 | 去掉嵌套同名目录，代码提到根目录 | 30 分钟 |

### P1 - 尽快做（影响可维护性）

| # | 任务 | 工作量 |
|---|------|--------|
| 6 | 拆分目录结构（app/、scripts/、tests/、models/） | 1 小时 |
| 7 | FastAPI 分层（api/schemas/services/core） | 2 小时 |
| 8 | 加统一响应模型 + 全局异常处理 | 1 小时 |
| 9 | 用 pydantic-settings 管理配置，阈值移到 .env | 1 小时 |
| 10 | print 替换为 logging | 30 分钟 |
| 11 | 删掉 .bak 文件、清理已追踪的 .idea/__pycache__ | 15 分钟 |
| 12 | Dockerfile 改用 uv | 30 分钟 |

### P2 - 有空就做（提升工程质量）

| # | 任务 | 工作量 |
|---|------|--------|
| 13 | 抽取 BaseProctor 基类，消除重复代码 | 2 小时 |
| 14 | 变量名改成 PEP 8 规范（去匈牙利命名） | 1 小时 |
| 15 | 魔法数字提取为常量 | 30 分钟 |
| 16 | 加 ruff 代码规范工具 + pre-commit | 30 分钟 |
| 17 | 写 pytest 测试 | 2 小时 |
| 18 | 写 README.md | 30 分钟 |
| 19 | 大文件用 Git LFS 管理 | 15 分钟 |
| 20 | 接口加 /api/v1 版本前缀 | 15 分钟 |

---

## 九、关键概念科普（给 Python 小白）

### pyproject.toml vs requirements.txt

- `requirements.txt`：老式依赖管理，就是个包名列表
- `pyproject.toml`：现代 Python 项目标配，能管理依赖、构建配置、工具配置（ruff/black 等）
- **结论：用 pyproject.toml，抛弃 requirements.txt**

### uv.lock 为什么必须提交

- lock 文件记录了「所有依赖（包括依赖的依赖）的精确版本」
- 提交它 = 任何人 clone 后 `uv sync` 都能得到和你一模一样的环境
- 不提交 = 每个人装的版本可能不同 = 「我这能跑你那不能跑」

### 虚拟环境 .venv 为什么不提交

- 虚拟环境是本地的、平台相关的（Windows 装的不能给 Linux 用）
- 别人 clone 后自己 `uv sync` 就能重建，不需要你的 .venv
- 提交了会让仓库巨大无比

### PEP 8 命名规范速查

| 类型 | 规范 | 例子 |
|------|------|------|
| 变量/函数 | snake_case 小写下划线 | `max_angle` |
| 类名 | PascalCase 大驼峰 | `ImageProctor` |
| 常量 | UPPER_SNAKE 大写 | `MAX_FACES` |
| 私有成员 | 前缀下划线 | `_internal_method` |
| **不要** | 匈牙利前缀 | ~~`m_iMaxAngle`~~ |

---

## 十、总结

你的项目核心逻辑是好的，阈值标定也做得专业。主要差距在「工程化」上——目录组织、依赖管理、配置管理、异常处理这些「脚手架」还没搭好。

建议按 P0 → P1 → P2 的顺序逐步优化，不用一次全改完。先把 P0 的 5 个问题修了（特别是提交 uv.lock），项目就能立刻上一个台阶。

有任何具体问题想深入聊的，随时问我。
