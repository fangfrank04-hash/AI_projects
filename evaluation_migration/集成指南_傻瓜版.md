# RAG 评测集成指南 — 傻瓜版操作步骤

> 目标：在不破坏 ragflow 原有代码的前提下，给每次 RAG 回答自动附加评测分数。
> 方法：改 1 个文件 + 建 1 个文件，共 2 个文件。

---

## 前提：先把评测包放到 ragflow 里

从 `evaluation_migration/` 目录，把 `evaluation/` 文件夹整体复制到 ragflow 项目根目录下：

```
D:\AI_projects\zhongzhai_pro\ragflow-main\
├── evaluation/        ← 拷过来的评测引擎
├── api/
├── rag/
├── ...
```

如果已经有 `evaluation/` 文件夹则跳过。

---

## 第 1 步：新建评测初始化文件

在 `D:\AI_projects\zhongzhai_pro\ragflow-main\api\` 目录下，新建一个文件：

**文件名**: `api/init_evaluation.py`

**完整内容**（直接复制粘贴）:

```python
# -*- coding: utf-8 -*-
"""
RAG 评测系统初始化
在 ragflow 启动时自动执行，初始化评测 LLM。
"""

import logging

logger = logging.getLogger(__name__)

_evaluator_ready = False


def init():
    """初始化评测系统。在 ragflow_server.py 的 if __name__ == '__main__': 里调用。"""
    global _evaluator_ready
    if _evaluator_ready:
        return

    try:
        from evaluation import set_llm_callable
        from api.db.services.llm_service import LLMBundle
        from api.db.services.tenant_llm_service import TenantLLMService

        # 获取默认的 chat 模型
        models = TenantLLMService.get_all_models(LLMType="chat")
        if not models:
            logger.warning("评测系统：未找到 chat 模型，跳过初始化")
            return

        # 使用第一个 chat 模型作为评测 LLM
        model_config = models[0]
        bundle = LLMBundle(model_config["tenant_id"], model_config, max_retries=0)

        set_llm_callable(bundle.chat)
        _evaluator_ready = True
        logger.info("评测系统初始化成功")
    except Exception as e:
        logger.warning(f"评测系统初始化失败（不影响正常功能）: {e}")
```

---

## 第 2 步：修改 `api/ragflow_server.py`（加 2 行）

**文件路径**: `D:\AI_projects\zhongzhai_pro\ragflow-main\api\ragflow_server.py`

**找到第 73 行**: `if __name__ == '__main__':`

**在它下面找到 `logging.info(f'RAGFlow version: ...')` 这一行（约第 85 行），在它**上面**加 1 行：

```
    logging.info(f'RAGFlow version: {get_ragflow_version()}')
```

改成：

```
    from api.init_evaluation import init      # 🆕 加这行
    init()                                     # 🆕 加这行
    logging.info(f'RAGFlow version: {get_ragflow_version()}')
```

---

## 第 3 步：修改 `api/apps/restful_apis/chat_api.py`（加 3 行）

**文件路径**: `D:\AI_projects\zhongzhai_pro\ragflow-main\api\apps\restful_apis\chat_api.py`

### 3a. 在文件顶部加 import（第 17 行 import json 下面）

```
import json
import logging
import os
import re
from evaluation import auto_evaluate    # 🆕 加这行
```

### 3b. 找到 `_format_answer` 函数（约第 1103 行）

原来的代码:
```python
        def _format_answer(ans):
            formatted = structure_answer(conv, ans, message_id, session_id)
            if chat_id:
                formatted["chat_id"] = chat_id
            return formatted
```

改成:
```python
        def _format_answer(ans):
            formatted = structure_answer(conv, ans, message_id, session_id)
            if chat_id:
                formatted["chat_id"] = chat_id

            # ===== 🆕 加下面 5 行 =====
            try:
                answer = ans.get("answer", "")
                chunks = [c.get("content", "") for c in ans.get("reference", {}).get("chunks", [])]
                if answer and chunks:
                    formatted["evaluation"] = auto_evaluate(
                        question=msg[-1]["content"] if msg else "",
                        answer=answer,
                        contexts=chunks,
                    )
            except Exception:
                pass  # 评测失败不影响正常功能
            # ===== 加完了 =====

            return formatted
```

---

## 完成！验证

重启 ragflow，问一个问题。在返回的 JSON 里会多一个 `evaluation` 字段：

```json
{
    "answer": "RAG 是...",
    "reference": {...},
    "evaluation": {
        "faithfulness": 0.85,
        "context_precision": 0.60,
        "context_recall": null,
        "answer_relevancy": 0.90
    }
}
```

---

## 常见问题

**Q: 报错 "evaluation 未找到"？**
A: 确认第 0 步中 `evaluation/` 文件夹已复制到 ragflow 项目根目录。

**Q: evaluation 字段为 null？**
A: 日志里会有 warning "评测系统初始化失败"，检查 LLM 模型配置。

**Q: 不想自动评测了怎么关？**
A: 注释掉 `ragflow_server.py` 里的 `init()` 那行，重启即可。
