# 项目方案书AI自动填写系统 - 项目状态总览 v2.0

> 更新时间: 2026-05-07
> 架构版本: Python主导 v2.0
> 当前阶段: V1开发 - 步骤1（团队职责）

---

## 一、架构变更说明

### 旧架构（已废弃）
```
前端 → Java Spring Boot → Python FastAPI → 通义千问
```

### 新架构（当前）
```
前端 ←SSE→ Python FastAPI(+agentscope) → 通义千问
                    ↓ 本地工具调用
                Mock数据 (需要替换为Java MCP服务)
```

### 架构反转原因
1. **领导要求**: 必须使用AgentScope最新版实现Skills和MCP功能
2. **技术选型**: 弃用LangChain，改用AgentScope + FastAPI + 通义千问
3. **配置驱动**: 采用steps.yaml + ChatEngine的配置驱动架构
4. **协议统一**: Python通过MCP协议调用Java数据服务

---

## 二、V1实现范围（当前聚焦）

### ✅ 包含内容
| 模块 | 说明 |
|------|------|
| 项目基本信息 | 查询、AI编辑（仅限productNo/productName）、一键回填 |
| 项目团队 | 查询、AI编辑（职责维护）、一键回填 |

### ⏸️ 延后至V2
- 步骤2: 建设目标
- 步骤3: 技术方案
- 步骤4: 实施计划
- 步骤5: 效益分析

> **注意**: V1代码需预留V2扩展接口

---

## 三、任务分解与进度

### 3.1 已完成任务 ✅

| 序号 | 任务 | 状态 | 产出物 |
|------|------|------|--------|
| 1 | PRD v2.0重写 | ✅ | `01-产品需求文档(PRD)-v2.md` |
| 2 | 架构设计文档v2.0 | ✅ | `02-架构设计文档-v2.md` |
| 3 | 数据库设计 | ✅ | `数据库设计文档.md` |
| 4 | MCP服务设计 | ✅ | `05-MCP服务设计文档.md` |
| 5 | Python后台代码框架 | ✅ | `proposal-python-v2/` |
| 6 | 前端代码修改 | ✅ | `react-frontend/src/` |

### 3.2 进行中任务 🔄

| 序号 | 任务 | 负责人 | 当前状态 | 阻塞点 |
|------|------|--------|----------|--------|
| 7 | 依赖安装与测试启动 | Claude | ✅ 已完成 | - |
| 8 | SSE流式输出验证 | Claude | ✅ 已完成 | - |
| 9 | Agent工具调用验证 | Claude | ✅ 已完成 | - |
| 10 | MCP独立服务改造 | Claude | ✅ 已完成 | - |
| 11 | Skills封装（前端逻辑→AgentScope） | Claude | ✅ 已完成 | - |
| 12 | Windows环境全链路联调 | Claude | ✅ 已完成 | 需DASHSCOPE_API_KEY |
| 13 | Java服务启动验证 | Claude | 🔄 构建中 | Maven依赖下载 |

### 3.3 待开始任务 ⏳

| 序号 | 任务 | 优先级 | 说明 |
|------|------|--------|------|
| 10 | MCP Server真实Java接入 | P1 | 替换mock数据，连接真实Java接口 |
| 11 | 联调测试与QA验证 | P1 | 端到端测试 |
| 12 | PRD数据字典更新 | P2 | 基于Java接口图片提取字段 |
| 13 | 文档完善 | P2 | 补充接口文档、部署文档 |

---

## 四、验收标准

### 4.1 功能验收标准

#### 4.1.1 项目基本信息维护
| 功能点 | 验收标准 | 优先级 |
|--------|----------|--------|
| 查询项目信息 | 能正确显示项目编号、名称、部门、级别、产品编号、产品名称 | P0 |
| AI编辑产品编号 | 通过自然语言指令修改productNo，修改后实时反馈 | P0 |
| AI编辑产品名称 | 通过自然语言指令修改productName，修改后实时反馈 | P0 |
| 权限控制 | 非PM用户只能查看，不能修改 | P0 |
| 一键回填 | 点击回填按钮，数据同步到前端表单 | P0 |

#### 4.1.2 项目团队维护
| 功能点 | 验收标准 | 优先级 |
|--------|----------|--------|
| 查询团队成员 | 能正确显示角色、姓名、职责列表（含勾选状态） | P0 |
| AI编辑职责 | 通过自然语言指令增删改成员职责 | P0 |
| 新增成员 | 通过自然语言指令添加新成员 | P1 |
| 删除成员 | 通过自然语言指令删除成员 | P1 |
| 权限控制 | 非PM用户只能查看，不能修改 | P0 |
| 一键回填 | 点击回填按钮，数据同步到前端表单 | P0 |

### 4.2 技术验收标准

| 检查项 | 验收标准 | 优先级 |
|--------|----------|--------|
| SSE连接 | 前端能成功建立SSE连接，接收流式事件 | P0 |
| 心跳机制 | SSE连接30秒无活动时自动发送ping | P1 |
| 错误处理 | 网络异常、服务异常时有友好提示 | P0 |
| 会话管理 | 刷新页面后需重新建立会话 | P1 |
| 响应时间 | AI响应首字节<2秒，完整响应<10秒 | P1 |

### 4.3 代码验收标准

| 检查项 | 验收标准 | 优先级 |
|--------|----------|--------|
| 代码规范 | 符合PEP8，有详细注释 | P1 |
| 错误处理 | 所有异常都有捕获和处理 | P0 |
| 日志记录 | 关键操作有日志输出 | P1 |
| 配置分离 | 环境相关配置抽离到.env | P1 |
| 接口文档 | FastAPI自动生成的docs可访问 | P2 |

---

## 五、文件清单

### 5.1 设计文档
| 文件名 | 状态 | 说明 |
|--------|------|------|
| `01-产品需求文档(PRD)-v2.md` | ✅ | Python主导架构PRD |
| `02-架构设计文档-v2.md` | ✅ | 三服务架构、接口契约 |
| `03-Java后台设计文档.md` | ✅ | Java数据服务设计 |
| `04-Python_AI后台设计文档.md` | ⚠️ | 需更新为v2版本 |
| `05-MCP服务设计文档.md` | ✅ | MCP协议设计 |
| `数据库设计文档.md` | ✅ | 数据模型设计 |

### 5.2 代码目录
| 目录 | 状态 | 说明 |
|------|------|------|
| `proposal-python-v2/` | ✅ | Python主导后台（当前主版本） |
| `proposal-python-v2/mcp_server/http_server.py` | ✅ 新建 | MCP独立HTTP服务（FastMCP, 端口8001） |
| `proposal-python-v2/mcp_client.py` | ✅ 新建 | MCP Streamable HTTP客户端 |
| `proposal-python-v2/skills/` | ✅ 已扩展 | 5个Skills（project_info/control_plan/schedule/resource/quality） |
| `proposal-python-v2/mcp_server/mock_data.py` | ✅ 已扩展 | 补充完整CRUD函数 |
| `proposal-python-v2/agent_setup.py` | ✅ 已重构 | 通过MCP协议调用数据服务 |
| `proposal-python/` | ⏸️ | 旧版本（废弃） |
| `proposal-java/` | ✅ | Java后台（数据服务接口） |
| `react-frontend/` | ✅ | React前端（已接入SSE） |

### 5.3 参考文件
| 文件名 | 说明 |
|--------|------|
| `Java接口.jpg` | Java数据接口截图1 |
| `java接口2.jpg` | Java数据接口截图2 |
| `补全后的前端代码.md` | 前端参考代码（Gemini示例） |

---

## 六、技术栈

### 6.1 Python后台
- **Web框架**: FastAPI >= 0.110.0
- **AI框架**: AgentScope (最新版)
- **LLM模型**: 通义千问 (qwen-max/qwen-plus)
- **通信协议**: SSE (Server-Sent Events)
- **MCP协议**: mcp (Python SDK)

### 6.2 前端
- **框架**: React
- **SSE客户端**: EventSource API
- **HTTP客户端**: fetch/axios

### 6.3 Java数据服务
- **入口**: `POST http://25.50.238.23:8088/portal/RestAction.invoke.do?url=xxx`
- **认证**: 本地开发使用mock，无需真实鉴权

---

## 七、接口清单

### 7.1 Python FastAPI接口

| 接口 | 方法 | 说明 |
|------|------|------|
| `/api/chat/stream` | GET | SSE流式连接端点 |
| `/api/chat/message` | POST | 发送用户消息 |
| `/api/chat/fillback` | POST | 一键回填 |
| `/health` | GET | 健康检查 |

### 7.2 前端→Python SSE事件

| 事件类型 | 方向 | 说明 |
|----------|------|------|
| `connected` | Server→Client | 连接成功 |
| `preview` | Server→Client | 初始数据预览 |
| `text` | Server→Client | AI文本回复 |
| `update_project` | Server→Client | 项目数据更新 |
| `update_team` | Server→Client | 团队数据更新 |
| `fillback_complete` | Server→Client | 回填完成 |
| `error` | Server→Client | 错误信息 |
| `ping` | Server→Client | 心跳包 |

---

## 八、风险与注意事项

### 8.1 已知风险
| 风险 | 影响 | 缓解措施 |
|------|------|----------|
| AgentScope版本兼容性 | 高 | 使用最新版，密切关注API变更 |
| MCP协议稳定性 | 中 | 先使用mock数据验证，再接入真实服务 |
| 内网环境依赖安装 | 中 | 提前准备离线依赖包 |

### 8.2 开发注意事项
1. **本地开发**: 使用mock数据，不连接真实Java服务
2. **权限控制**: 所有修改操作必须检查isPM标志
3. **错误处理**: 工具调用失败时向用户报告，不重试超过2次
4. **数据一致性**: 修改后重新获取数据，确保前端显示最新状态

---

## 九、下一步行动

### ✅ 已完成（2026-05-07 下午）
- [x] 安装Python依赖: `pip install -r requirements.txt`
- [x] 前端业务逻辑封装成5个AgentScope Skills
- [x] MCP Server改造为独立HTTP服务（FastMCP, Streamable HTTP, 端口8001）
- [x] MCP Client实现（JSON-RPC over SSE）
- [x] Agent重构：通过MCP协议调用数据服务
- [x] mock_data.py 补充完整CRUD函数
- [x] SSE流式输出验证通过（connected → preview → text）
- [x] Agent工具调用验证通过（8个工具全部注册）
- [x] MCP Client → MCP Server全链路通过

### ⏳ 待完成
- [ ] 设置 `DASHSCOPE_API_KEY` 环境变量（启用AI对话功能）
- [ ] 启动前端验证: `cd react-frontend && npm run dev`
- [ ] Java服务完整启动验证: `cd proposal-java && mvn spring-boot:run -Dspring-boot.run.profiles=dev`
- [ ] 端到端手动测试（AI对话、数据修改、一键回填）
- [ ] 更新PRD数据字典

### 启动命令
```bash
# 1. 启动MCP数据服务（端口8001）
cd proposal-python-v2 && python -m mcp_server.http_server

# 2. 启动FastAPI主服务（端口8000）
cd proposal-python-v2 && python -m uvicorn main:app --host 0.0.0.0 --port 8000 --reload

# 3. 启动前端（端口5173）
cd react-frontend && npm run dev

# 4. 可选：启动Java服务（端口8088）
cd proposal-java && mvn spring-boot:run -Dspring-boot.run.profiles=dev
```

- **项目路径**: `D:\AI_projects\zhongzhai_pro\project-proposal-ai`
- **Python后台**: `proposal-python-v2/`
- **前端代码**: `react-frontend/`

---

*本文档随项目进展持续更新*
